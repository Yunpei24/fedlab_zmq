# Versions tested locally on CPU. Install CPU torch/torchvision wheels through
# the cluster-approved package source; Linux/Toubkal validation remains required.
torch==2.11.0
torchvision==0.26.0
numpy==2.4.3
PyYAML==6.0.3
pytest>=7
